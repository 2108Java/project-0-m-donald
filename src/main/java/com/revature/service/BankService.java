package com.revature.service;

import com.revature.models.Account;
import com.revature.models.User;

public interface BankService {

//	public Account[] getAllAccounts(); // Returns an array of accounts
//	
//	public User[] getAllUsers();
//	
//	public boolean viewBalance(double balance);
//	
////	public boolean isEmployee(boolean isEmployee);
//	
//	public boolean applyForApplication(User newUser); // Let's a user apply for an application
	
//	public boolean loginToAccount(String username, String password); // returns username and password to let a user login to an account 
//	
//	public boolean withdrawMoney(int custID, int amt, double balance);  // Uses customer id to withdraw money from the account and display new balance
	
//	public boolean depositMoney(int custID, int amt, double balance);

//	public boolean updateApplicationStatus(int accountNum, String username);
//
//	public boolean verifyEmployment(String username, boolean isEmployee);
}
